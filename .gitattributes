# Normalize line endings: LF in the repository, native in the working tree.
# Keeps the diff clean across Windows / macOS / Linux contributors.
* text=auto eol=lf

# Binary assets — never touch these.
*.png     binary
*.woff2   binary
.thumbnail binary
